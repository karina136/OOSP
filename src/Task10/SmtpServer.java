package Task10;

public class SmtpServer {
        public void configureServer(String host, int port) {
            System.out.println("SMTP-сервер настроен: " + host + ":" + port);
        }
}

